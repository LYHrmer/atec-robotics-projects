Write `/home/lybm/ATEC_Robotics_Projects_20260910/task_b/payload_motion.py` ONLY. Actual Claude Opus implements this independent file. Do not write memory, tests, any other production file, original environment/physics/reward files, Git, GPU or network. Root owns evaluator integration and a separate agent owns audit. Prefer a compact implementation with a reusable arm tracker and a thin policy composition, not a general framework.

## Purpose and public interface

Implement `PayloadMotionPolicy(schema, observation_joint_names, defaults, dt=.02, *, settle_calls=100, ramp_calls=100, reach_only=False, forward_cmd=.20, turn_cap=.20, standoff=.56, turn_gain=.4, lowering_m=.02)` with the same caller keyword interface as ContactGraspPolicy. Compose the NEW `task_b.contact_grasp.ContactGraspPolicy` with these exact parameters (lowering_m=.02, only .02 allowed by that child), and call it exactly once per tick while it runs. Do not subclass or copy it. Expose `act(proprio, images)`, `state`, `done_reason`, `debug`, `alpha`, `pause_for_stance`, `wheel_hold_requested`, and `describe()`. No extra required evaluator arguments.

Only `grasp_probe_observation_complete` opens the payload sequence. Every other child failure/stop is propagated without retry, reset, alternative target or score consultation. This normal child reason itself is NOT proof of grasp: the real run will only be enabled after an independently verified physical grasp candidate, and every resulting payload run will still undergo actual-object audit. FirstReach, GraspProbe and ContactGrasp source files are read-only. ContactGrasp is being written by a separate actual Opus session; its fixed public interface includes inherited `lift_start_q`, `lift_goal`, `arm_command`, and the normal `grasp_probe_observation_complete` reason. You can implement against this agreed interface without changing or waiting for that file.

Before child normal completion, return the original child actions exactly, including the normal-completion call; preserve original wheel-hold and stance semantics. Afterwards STOP calling the child permanently: its completed 25 s contact/12 s probe timers must never run again. Do not clear its normal done_reason or any child failure. Your wrapper alone now advances independent finite payload clocks. Hold the SAME wheel anchor throughout this stationary payload experiment. Reuse the final child leg action/reference without altering lowering, original actuators or StanceHold. Save the EXACT final child action as a template (its leg action contains BOTH the child .02 lowering AND ContactGrasp extra .02). Zero the wheel slice and replace only the arm slice via the existing physical-target action mapping. Do not reconstruct legs from `_inner.lower_delta`, which would drop the extra lowering. The finger physical closed targets remain the child targets; never open fingers in this module.

Expose the waypoint construction and single-waypoint arm tracker as small reusable pieces in the SAME file so a future delivery wrapper can insert navigation after waypoint A, then request B/C near the bucket. The default PayloadMotionPolicy runs A, B, C and a2 s final observation while stationary; it does not implement navigation, release, delivery or scoring.

## Exact reusable interfaces in this ONE file

Implement these small named units so future delivery navigation does not need to copy your controller:

- `build_payload_goals(reference_q, current_q) -> dict`: finite six/eight arm coordinates accepted and sliced to6; returns `A`, `B`, `C` as six-axis numpy arrays and `diagnostics` containing actual IK errors and formula parameters. It is a pure static function: no simulator/policy mutation, no world object input. Validate before returning; raise ValueError on invalid geometry, limits or IK failure. Wrapper catches and stops before new movement.
- `PayloadJointTracker(dt, initial_reference, initial_command)`: six-axis vectors; initializes filtered holding correction from command-reference as specified below, keeps immutable static limits and current finite physical command. It owns scalar reference interpolation and selected-axis feedback only, not body safety, fingers, wheels, navigation or success claims.
- `tracker.begin(goal)`: sets ONE fixed six-axis goal and scalar start to the previous desired-actual reference, resets only scalar alpha/duration. Does NOT reset command or filter. Validate goal/limits atomically. The owner permits new segments only after actual arrival. Expose `planned_duration_s` for the owner deadline.
- `tracker.update(q, qdot, *, advance_path=True, paused=False) -> ndarray[6]`: validate finite actual six-axis inputs. If paused, return the same command and freeze alpha/reference/filter; no hidden catch-up. Otherwise advance scalar alpha by dt/planned_duration only when advance_path is True, apply one filtered correction update, then intersect rate/tether/hard limits. `advance_path=False, paused=False` means continue load-compensated tracking of the SAME current reference; this is the A-complete holding mode for future navigation. It is not a body-motion waiver. If an intersection is empty, keep last command, set a stable `done_reason`; subsequent updates cannot revive it.
- Expose tracker `reference`, `command`, `fixed_goal`, `path_alpha`, `raw_correction`, `filtered_correction`, `done_reason`, `planned_duration_s` and `describe()` with numeric constants. Arrays returned in descriptions must be serializable copies; do not expose mutable internal history unintentionally.
- `PayloadMotionPolicy` owns public proprio parsing, ContactGrasp prefix, phase ordering, current action template/fingers, actual quiet windows, body/pause/jaw guards and WALL deadlines. It uses one tracker instance continuously A->B->C->HOLD. Avoid a second implementation of the arm update in the policy.

Default stationary behavior executes all three segments. The future delivery owner can directly reuse goals+tracker, complete A, keep `advance_path=False` with active feedback while handling its own separately audited navigation guards, and call begin(B)/begin(C) later. No external callback/controller framework or third-party plugin is needed.

## Waypoint formulas: public measured reference and static B-mounted FK/IK only

Use the child's `lift_start_q` (public measured arm q latched after closure) as `reference_q`. Use the current public q as IK seed and feedback. Never read object position/orientation, base world pose, score, contact force or a private experiment report at runtime. Import `fk` and `solve_ik` from task_b.arm_kinematics and the original JOINT_LOWER/JOINT_UPPER from task_e_geometry (only shared static Piper limits, no Task E scene pose).

Let T0=fk(reference_q), R0=T0[:3,:3]. Set d=normalize(T0[:2,3]-[.2,0]); reject a degenerate direction. Use scipy Rotation or a correct Rodrigues helper; all rotations below are BODY-fixed-axis LEFT multiplication:

- A (raise before any side swing): position pA=[.2+.35*d_x, .35*d_y, .45], axis nA=[-d_y,d_x,0], orientation RA=Rot(nA,-.20) @ R0. Solve bounded IK with current public q as seed.
- B (swing only after A actually settles): copy qA and replace ONLY joint1 with +pi/2. Other five desired joint coordinates remain qA.
- C (extend over the side at height): pC=[.2,.52,.40], RC=Rot([-1,0,0],-.40) @ Rz(pi/2-reference_q[0]) @ R0. Solve bounded IK, seeded by qB. C's -.40 is relative to R0 with the specified yaw; do NOT add it again on top of A's -.20, and do NOT right-multiply around a gripper-local axis.

Validate all generated goals, joint limits, IK position error<=.005 m and orientation error<=.04 rad. A failed plan stops before new motion; no fallback hardcoded posture. Report all planned goals/IK errors and formula parameters in describe/debug. The example q vectors in private geometry reports are NOT runtime constants.

## Scalar path reference and bounded load compensation

The initial desired-actual reference is the completed child's SAME `lift_goal`; it already passed that child's actual-goal admission. Do not silently replace it with an actuator command or a fresh biased measurement. The first previous actuator command is the child's final `arm_command`. This preserves the load at handoff.

For each A/B/C segment, interpolate one scalar alpha from previous desired-actual goal to the next fixed goal. Nominal max axis rate=.10 rad/s, so scalar duration=max(abs(goal-start))/.10. Reference alpha freezes during a pause and is bounded0..1. Do not advance each axis independently and thereby abandon the audited scalar path. After a segment actually settles, the next segment starts from that prior fixed desired-actual goal; actual q remains feedback.

On every ACTIVE tick, compute P correction against the current scalar desired-actual reference, only on q2/q3:

- q2 gain4, raw correction cap+/-.14 rad, measured-command tether+/-.18 rad.
- q3 gain2, raw correction cap+/-.08 rad, measured-command tether+/-.10 rad.
- Other axes have zero added P correction and original+/-.10 rad measured-command tether.
- Filter correction with tau=.10 s and exact weight1-exp(-dt/tau). At the initial child-to-payload handoff initialize selected-axis filtered correction from `child.arm_command - child.lift_goal`, clipped to the corresponding correction cap; this avoids dropping the holding reference. Keep filter history across A/B/C and final hold. Only pause freezes filter and command; do not zero load compensation at a phase boundary.
- Candidate actuator target=scalar reference+filtered correction. Intersect per-axis previous-command+/-.10*dt, measured-q+/-selected tether, and original hard joint limits. If ANY intersection is empty, stop and hold the previous command. No order-dependent clipping, silent bound expansion, goal shifting or actuator-gain changes.

The original final finger targets and commands must remain continuous. The arm tracker must keep running the same desired-actual goal closed loop in segment settling and final observation; do not freeze a loaded actuator command merely because alpha reached1.

## Completion, timing and finite stops

Each segment may complete only after alpha=1 AND actual max six-axis error to its FIXED GOAL<.04 rad, every-tick arm qdot<=.12 rad/s, and a full .5 s(26 samples at50 Hz) measured-position window with six-axis range<=.002 rad. Retain original body quiet conditions: tangent speed<.01, raw linear norm<.06, angular norm<.12, tilt<=.10; leg measured-position window range<.02 over the same quiet period. Clear admission history after phase change, pause or a failed prerequisite. Do not require the old instantaneous qdot<.05 in this new module; this is the explicit position-window-plus-hard-rate replacement supported by real quiet traces. No object-state completion gate.

Per-segment WALL deadline=planned scalar travel duration+6 s; it includes pauses/settling and is never refreshed. Require at least .01 rad reduction of remaining actual fixed-goal error over3 s while that error>.04, but reset this progress comparison after a legitimate body-rate pause (do not reset the deadline). If the commanded scalar path has meaningful verified progress, do not mistake an interpolation bookkeeping change for a stall.

Keep a finite body-rate settle response: raw linear>=.06 or angular>=.12 pauses motion; freeze path/filter/command, require .2 s consecutive below-threshold samples to resume. Single pause episode<=2 s, cumulative pause<=6 s for the entire payload experiment. Original hard tilt .25 and instability tilt>.12 or roll/pitch-rate norm>.45 stop immediately. Track public tangent displacement/yaw/gravity-direction change from payload entry and stop above .03 m/.05 rad/.05 rad, without integrating vertical drift. Reject nonfinite/malformed original proprio84. An unexpected new stance recalibration stops the payload sequence. All body displacement/yaw/gravity anchors in this module start once at the legitimate child-normal handoff; they are independent payload budgets, not a reset of a failed child. Inherited child safety outcomes remain frozen evidence and are never reclassified.

Measured jaw width<.006 m for .2 s stops as an empty-gripper indication. It never certifies attachment when width is larger. Fixed fingers continue holding, even on stop. Final PAYLOAD_HOLD continues the C closed loop for2 s of valid quiet observation; use a5 s absolute final-hold deadline. Compute one total finite payload deadline from the three original segment budgets plus5 s at plan creation; never refresh it on phase transitions. No silent retries.

Public phases: PREFIX / PAYLOAD_RAISE / PAYLOAD_SWING / PAYLOAD_EXTEND / PAYLOAD_HOLD / STOPPED. Normal completion reason `payload_motion_observation_complete` is a sequence-completion claim only. Describe/debug must expose child phase/reason, reference_q, scalar reference/alpha, fixed goal, actual error, command, raw/filtered compensation, all caps/tethers, q/leg window durations/ranges, pause/deadline/progress diagnostics and wheel hold. The wrapper `done_reason` remains None through the child's intercepted normal completion and only becomes non-None on its own completion/failure.

## Independent evidence and required checks

Static original USD/full bottle geometry at the EARLIER contact point under a RIGID ATTACHMENT ASSUMPTION found the candidate C at bucket-center distance1.45 m gives full bottle minimum z≈.6307 m (rim .55), root radius≈.8512 m, all8377 bottle vertices radius<=.9005 m, and body/four-wheel outside-wall support-plane clearance≈.0744 m. A then B then C scalar samples avoided payload/non-arm AABB overlap. These are geometry checks, NOT actual grasp/carry/collision guarantees and must not become runtime GT constants. The new +30 degree contact wrist angle is automatically included by using the NEW child lift_start_q in R0; never substitute the old example q. The deeper contact changes the hypothetical attachment as well: old numerical clearances are evidence for planning only and require independent recheck for the new actual grasp. No object attachment matrix is supplied to runtime.

For arbitrary body tilt<=.12 and .5 kg payload COM within the original bottle mesh, sampled static torque upper bounds were q2≈13.60069 Nm/q3≈6.56302 Nm. OriginalK80 implies offsets .170009/.082038 rad. Selected feedback predicts fixed-goal residuals .03400/.02735 and corrections .13601/.05469 within the chosen caps. Original effort limit100 remains unchanged; acceleration, contact and imperfect attachment are not guaranteed by static bounds.

Independent audit owns tests: exact parameter-matched ContactGrasp prefix and failure propagation; dynamic formulas/IK limits; selected-axis compensation, filter handoff continuity, scalar trajectory and .10 command rate; tether/hard-limit intersection and infeasible stop; pause history/no deadline reset; loaded models from the stated torque offsets; position-drift and emergency-rate rejection; full quiet-window admission; fingers/legs/wheel anchor continuity; A-before-side-swing dependency; bounded hold completion; no GT/reward input. Do not edit the tests or relax the fixed actual-goal thresholds to satisfy them.

Implement now in only payload_motion.py, then py_compile that file. Do not run the GPU or claim grasp, high-lift or delivery success. Report exact files and CPU validation actually completed. The root will gate real payload execution on independently verified physical grasp.
