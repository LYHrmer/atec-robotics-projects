Implement ONE bounded, opt-in two-target proximity experiment for Task B.

WRITE PERMISSION: ONLY /home/lybm/ATEC_Robotics_Projects_20260910/task_b/multi_reach.py. Do not write tests, other code, docs, memory, user/project notes, shell files, configuration, or any external file. Do not run GPU, start Isaac, change the official environment, use Git, invoke another agent, or expand this into a navigation framework. Read the frozen first_reach.py and helpers as necessary. Root owns evaluate.py; the independent reviewer owns audit_multi_reach.py. Finish with a short implementation report; do not claim simulation success.

Public interface already integrated by root:
MultiReachPolicy(schema, observation_joint_names, defaults, dt=.02, *, settle_calls=100, ramp_calls=100, reach_only=False, forward_cmd=.05, turn_cap=.6, standoff=.5, turn_gain=2., lowering_m=.02, max_attempts=2).
Expose act(proprio84, images), state, done_reason, debug, alpha, pause_for_stance, describe(), and boolean property wheel_hold_requested. Root passes the same public inputs as FirstReachPolicy and applies the existing stance/hold/drive layers. Output remains normalized action24; physical wheel gain and scale are NOT applied in this module. Root's max_attempts range is 1..3, though the next real experiment uses 2. Reject reach_only=True for this experiment. No score/reward/GT field is passed in or accessed.

Objective and frozen first attempt:
Use composition or a small private subclass to reuse FirstReachPolicy; do not edit or duplicate its full controller. Until its first normal reach_lowering_hold_complete, the first attempt's requested actions and guards must be identical to FirstReachPolicy with identical parameters and public inputs. Passive visited-point bookkeeping and extra diagnostics must not affect first-attempt actions. Any other child done_reason propagates immediately as a terminal policy reason. Never clear an error or restart after a failed guard. Do not transition when a reward appears. The evaluator will run with score_hold_steps=0. A completed attempt means the controller finished its bounded reach and hold; it does not mean the policy knows it scored.

Normal completion and stationary recovery:
Only intercept reach_lowering_hold_complete. Count one completed attempt. If it reaches max_attempts, set done_reason=multi_reach_attempts_complete and stop holding the current target. Otherwise perform the following once, with continuous wheel_hold_requested=True from the original BRAKE anchor through RESTORE_HEIGHT, RETRACT_FOR_VIEW and CONFIRM_NEXT_TARGET. Do not release/re-engage or re-anchor wheels during these phases. First normal-finish tick holds the previous arm/leg command without a discontinuity; do not call a child twice in one control tick.

1. RESTORE_HEIGHT: preserve the final arm/finger command and reduce lowering_alpha monotonically to zero at at most dt/3 per tick, using the same existing lower_delta and action scale. This reverses only the previous 2 cm reference, not a new stance reset or calibration. Validate actual leg response against the original authorization reference_q0 + alpha*lower_delta; increment error >.06 rad for .2s stops. At alpha=0 require .5s (25 control observations at dt=.02) of actual leg window range<.02 rad, arm goal error<.04 rad, arm qdot<.05 rad/s, tangent speed<.01m/s, linear norm<.06, angular norm<.12 and tilt<=.10 before retracting. Total height-restoration budget 6s, including pauses and the final quiet window.

2. RETRACT_FOR_VIEW: latch q1 from public measured arm q at entry, clipped only to genuine static position limits after validation, and set six-axis view goal [q1,1.1,-.8,0,0,0]. Preserve open finger commands, never close a gripper. Move each axis <=.30rad/s and tether commands within measured q +/- .10rad, like frozen reach. Require actual max goal error<.04rad and arm qdot<.05rad/s for .5s before selection. Retraction maximum 12s; every full 3s window with error>.04 must improve maximum error by >=.01rad, excluding finite pause intervals. Do not count command arrival as actual arrival.

During both recovery phases: validate finite public84; original hard tilt .25 and original reach instability tilt>.12 / roll-pitch rate norm>.45 still stop. Keep a common stationary public budget across restoration, retraction and initial confirmation: net tangent displacement<=.03m, |yaw integral|<=.05rad, gravity direction change<=.05rad. Linear norm>=.06 or angular norm>=.12 pauses motion, never raises thresholds: at most 2s per pause, 3s total across these phases, .2s continuously quiet to resume. Phase deadlines and drift budgets keep counting. Freeze alpha/arm command during pauses. Do not describe these conditions as proof of zero body motion.

Visited points and new visual selection:
Maintain only locally propagated positions of visually attempted targets; never IDs, world pose, a seed-specific map, score flags, or simulator state. Capture each child's fresh reach_target at REACH_READY and propagate it once per outer control tick. It becomes excluded after that attempt normally completes. Use public up=-gravity/|gravity|, tangent velocity v-dot(v,up)*up, and yaw component omega_yaw=dot(omega,up)*up to propagate static local points p -= dt*(v_tangent+cross(omega_yaw,p)); compare distances after projection into the current gravity tangent plane. This is bounded local dead reckoning, not ground-truth localization. Do not integrate public vertical velocity into height. Debug must label this approximation. Keep records for at most max_attempts; never overwrite visited memory when a child is reconstructed.

3. CONFIRM_NEXT_TARGET: keep view goal/zero lowering and the same wheel anchor. Collect real candidates from existing original RGB-D arrays and unchanged default detect_yellow_candidates; this module may reuse the detector and camera FK but must not change global detector behavior. Only for a NEW second-or-later target acquisition require metric height_span_m>=.12m, forward x>.8m, finite supported depth, and tangent distance from every completed point>=.50m. This is a tall-yellow-component heuristic, not mustard classification. Merge same-camera temporally matching candidates, select smallest absolute bearing plus .035*planar distance as the existing controller does. Use a fresh StationaryTargetGate / equivalent strict two-new-sample confirmation while base+arm quiet, with sensor period .10s, pair tolerance .04m and max age .50s; no fake camera frame IDs or content-change requirement. The first confirmation must occur after restoration/retraction quiet completion, not during arm movement. Maximum initial confirmation wait 8s; if there is no eligible newly confirmed target, stop next_target_not_confirmed. Do not blindly scan/spin or relax initial height/depth gates.

Once this new target is confirmed, retain association to its publicly propagated local point. The initial .12m height requirement is NOT applied again during near-field approach or BRAKE; partial views of a confirmed bottle can have smaller spans. Keep visited exclusion and do not silently switch to an arbitrary third target when this association is lost. Missing observations use existing stop/no-valid-target timing. The next BRAKE must still obtain the original fresh stationary confirmation; initial selection does not authorize a reach.

Avoid driving through the completed target:
Let d be the new target vector projected into the gravity plane, u=d/|d|. For every completed p in that plane, compute along=dot(p,u), clearance=|p-along*u|. A blocker has 0<along<|d| and clearance<.30m. This uses only remembered public visual points. If no blocker, hand the goal to the next child. Otherwise enter BACKOFF_CLEARANCE before navigation.

4. BACKOFF_CLEARANCE: release wheel hold only when this commanded movement begins. Keep the raised leg reference and verified view arm goal. Request all four wheel actions equally -forward_cmd (next run .05), with existing .5 normalized units/s wheel slew; never scale by drive gain here. No differential command and no unseen long-range reverse path. Track backward distance from public tangent velocity projected onto entry forward direction; at most .45m or 6s. Recompute blocker clearance from propagated points each tick. Stop backing as soon as clearance>=.30m (hysteresis may use .32m to avoid chatter); if the distance/time limit is reached while still blocked, stop backoff_clearance_not_reached rather than navigating through the blocker. If backward progress<.01m in a full 3s window, stop. Preserve original tilt and roll/pitch-rate stops; deliberate backward speed is NOT tested against the stationary .06m/s condition. Bound public tangent speed<=.25m/s, yaw change<=.05rad and gravity direction change<=.05rad during this short straight reverse. Invalid observations or out-of-bound motion stop immediately. Do not reset an error into a fresh attempt.

5. RECONFIRM_NEXT_TARGET: stop wheel motion and engage a NEW wheel anchor after BACKOFF (this anchor change is allowed because deliberate travel occurred). Hold the view arm/raised leg pose. Wait at least 1s and the same .5s public quiet conditions, then obtain a fresh two-sample confirmation of the SAME propagated new goal using the unchanged detector and .35m association gate; still exclude old points but do not require .12m span again. Maximum 8s. If it cannot be confirmed or the .30m straight-line clearance has become blocked, stop; no repeated backoff loop in this experiment.

Second child handoff:
Construct/reset only per-attempt FirstReachPolicy state for the already raised, verified view posture. Never re-run initial settle calibration or compact transition; root's stance modules and calibration stay alive. Use settle_calls=0, the same ramp_calls for bounded wheel restart, same forward/turn/standoff/lowering settings, current actual view arm command, stance_verified=True, near_view=True, and a fresh confirmed target with valid local timestamp. All reach/lowering gates, counters, progress, stationary confirmation and deadlines must start fresh for this target; previous visited memory survives. The child may be a private subclass with only candidate filtering/association overridden for later attempts; preserve the default camera extraction and detection algorithm. The next child must receive actual public observations, never sampled GT or an IK target derived from an object map. Release the temporary confirmation wheel anchor only when normal approach starts; the child's BRAKE will engage its own anchor. Do not let the legacy target propagation or wrapper propagate the same child target twice.

Diagnostics:
Expose attempt_index (1-based), completed_attempts, max_attempts, global_control_calls and child_local_calls, outer_phase, child_state, wheel_hold_requested, visited local vectors and their approximation basis, current new-goal association, selection rejection reasons, clearance, backward distance/elapsed, height/retract error and progress, quiet counts, phase deadlines and any pause budget. describe() must state no implemented grasp/delivery and that completed attempts do not certify score. Do not report GT object12, known world coordinates or predicted score in production fields.

Independent checks the reviewer will supply (do not write them):
- max_attempts=1 and first-attempt prefix match original actions; first-phase detector calls and kwargs unchanged.
- Only normal lower-hold completion enters recovery; every error propagates, even on the second attempt.
- Height restoration is monotone bounded, no arm jump/finger close, leg response checked, wheel anchor requested continuously.
- Measured q/qdot gate retraction, speed/tether bounds, pose/rate pause timeouts and deadline retention.
- Repeated/stale camera data cannot authorize selection; old-point exclusion and source changes work; new tall candidates admitted, near-field span reduction after association not rejected by the acquisition-only threshold.
- Public yaw/tangent propagation preserves a static point under a synthetic forward/turn trajectory; no vertical-rate or simulator/reward inputs.
- Blocking geometry triggers one bounded reverse, release/engage occurs only around deliberate travel, no bypass at timeout or after failed reconfirmation.
- Next child starts raised and with measured view pose, never resets the root stance holders, and needs a new BRAKE gate; two normal completions end the bounded experiment regardless of score.

Scope and first real experiment:
Root runs a fresh process, seed42, max_steps15000, mode multi_reach, reach_attempts2, existing compact/stance_hold/brake_wheel_hold/gain8/forward.05/turncap.6/turngain2/standoff.50/lower.02, score_hold_steps0, original official stop conditions unchanged. Real acceptance requires distinct original one-time proximity events for >=2 different objects, no original failure, complete artifacts and independent audit. No claim that the second target will necessarily be visible or the new reverse/retract path collision-free; CPU kinematics is only a precheck. If any new guard fails, stop and retain the evidence instead of repeatedly extending budgets.
