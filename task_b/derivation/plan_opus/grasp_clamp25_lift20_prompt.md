Implement this single bounded candidate in `/home/lybm/ATEC_Robotics_Projects_20260910/task_b/grasp_probe.py` ONLY. Do not write memory, tests, other files, original simulator configuration, or Git. No GPU or network calls. Read existing source and make a small edit; do not rewrite the policy.

1. Set `CLOSE_PRELOAD_M = .025`, retaining `gripper_targets(0., preload_m=CLOSE_PRELOAD_M)`. This existing helper permits .025. Physical targets are q7=-.025 m, q8=+.025 m. No extension of the helper, original joint stops, action clipping or actuator settings.
2. Set `PAIRED_DELTA_RAD = .20`: latch the same public measured q once after the existing closure admission, then q2 -= .20, q3 += .20. Preserve goal validation against original static limits. Never silently clip or redefine the measured goal.
3. Keep finger slew .05 m/s, close minimum active 1.4 s, maximum 3 s, target-commanded requirement, and the original 10-sample/.002 m stable nonempty-width gate. From fully open .035 to -.025 the nominal slew takes 1.2 s; the width window then occupies .2 s. Admission still depends on actual measurements.
4. Keep current q2 position feedback gain 1, correction cap .08 rad; arm command .10 rad/s and the intersection of previous-command slew, measured q +/- .10, and original limits. Preserve infeasible-intersection stop.
5. Keep arrival against the original fixed measured goal: actual maximum error < .04 rad, qdot < .05 rad/s, public gravity-projected pinch rise >= .015 m continuously .3 s. Keep LIFT 5 s, OBSERVE 2 s, total probe 10 s, and every pause, empty-width, body-motion and progress stop. Total deadline takes priority; do not extend it to accommodate subphase maxima.
6. Frozen FirstReach prefix, wheel anchor continuity, fixed lowered leg reference, observation inputs and evaluator interface remain identical. No reward, object pose, contact sensor or other privileged data enters control.
7. Update stale prose and metadata to agree with these constants; avoid hardcoded old .015-preload/.10-lift descriptions. Identify this as a bounded clamp/lift candidate, not successful grasp. If describing pressure, call it a nominal spring estimate only, never measured contact force or guaranteed grip.
8. Return a concise change list and compile result. Independent audit is owned by another agent; do not edit or bypass its checks. Required checks include prefix equality, new physical target mapping, preserved slew/tether/hardlimit intersection, fixed actual-goal thresholds and original stop behavior.

Planning evidence, not policy inputs: official Task B Mustard_cfg overrides USD mass to .5 kg. Attached B2w arm uses stiffness 80, damping 4, max effort 100 for arm_joint.*, including fingers. Task B disables the base material-randomization event; scene default static/dynamic friction are both 1 with multiply combination. Neither parsed mustard nor combined B2w/Piper asset has authored PhysicsMaterialAPI. These do not certify actual contact force. At 34.6 mm jaw width, the zero-velocity sum of spring-force magnitudes is about 5.17 N with .015 preload and 6.77 N with .025; actual contact geometry, damping and slip matter. The previous run's bottle remained supported even though the jaws rose. A larger lift is a bounded test of attachment versus slip.
