Implement the following narrowly specified GPT-6-Astra ultra plan now. You are the actual Claude Opus coding worker. Work in /home/lybm/ATEC_Robotics_Projects_20260910. Only edit task_b/first_reach.py, task_b/visual_approach.py, task_b/audit_first_reach.py. Do not change official environment, evaluator, wheel hold, Git, docs, other files; do not launch simulations. Read current files first. Keep the change small. Root will run tests and GPU. You have Read/Glob/Grep/Write/Edit tools.

Evidence: latest real original-env plan_p1_hold_seed42_01 reached REACH step2341 then stopped at2369. Brake hold fixed rollback to1.61cm. Target actually remains visible: final PNG yellow component bbox[153,212,68,69],area3063,aspect1.0147. Global aspect>=1.15 rejects it in overhead view. Initial localization used two stationary raw depth detections; this is enough for one bounded zero-lowering joint reach; continued shape detections must not become an impossible condition.

Astra's exact execution contract:
1. visual_approach.detect_yellow_candidates gets keyword locked_target_body=None. None preserves all current search behavior. Non-None validates finite shape(3,), bypasses only the aspect1.15..6 condition; retains area>=35, h>=10, depth, cloud, metric height and forward region filters. After computing body_point require distance to locked_target_body<=.12m. Debug reports locked association mode; it does not claim semantic identity. Unmatched distant components are ignored, never mistaken for tracked-object movement.
2. first_reach._detect passes locked_target_body only after reach_q has been authorized by the stationary gate. Fixed reach_target and reach_q never retarget or re-solve IK during reach. Initial approach/BRAKE detector filtering remains identical. Keep diagnostics of detector rejections per camera for visibility diagnosis.
3. _reach: for lowering_m==0 and actual stationary gate authorization (reach_only synthetic case already separate), missing candidates, shape rejection, unavailable images, or out-of-frame only log diagnostic status; do not enter half-second REACH_VISUAL_HOLD. Execute the existing SAME joint goal with existing .30rad/s slew, measured q±.10 tether, dynamic total deadline, 3s progress guard, tilt and public base velocity hard exits. Do not relax those. lowering_m>0 retains old strict logic: this task does not authorize lowering. Don't delete the old stricter branch.
4. At REACH_READY save normalized public gravity/up anchor, zero a 3D tangent displacement integral and yaw integral. Each subsequent real reach call integrate v_plane=v-(v dot up)*up times dt, and omega dot up times dt. Finish on norm(net displacement)>.03m, abs(net yaw)>.05rad, or gravity direction vs anchor>.05rad. Explicit names e.g. reach_base_displacement_budget_exceeded / reach_base_yaw_budget_exceeded / reach_gravity_changed. Record approximate public estimates and budgets in debug; this is small-angle local-frame integration, not world truth. Never integrate vertical velocity to pursue score. Existing instantaneous velocity stop stays. Tests exercising ready() initialization must cover these fields.
5. New associated raw points can update visual diagnostics but cannot change fixed reach_target/reach_q. Do not call distance>.12 unmatched a confirmed target movement. Keep old strict lowering-loss behavior to prevent accidental P2 enablement.
6. Update describe() contract honestly: stationary initial visual localization then ONE bounded joint feedback reach; shape-assisted local tracking is optional; no continuous identity/vision guarantee and no physical grasp/score claim.

Update audit_first_reach CPU checks meaningfully (current tests have now-obsolete assertions that missing in-view detection must halt all P1). Preserve all stationary gate, stance, actual q mapping, soft-vs-hard arm limit, synthetic probe, near vs far and existing safety checks. Add default detector round-shape rejection vs locked acceptance with a depth-supported synthetic image/cloud, distant locked association rejection, >.5s missing vision still makes bounded joint progress, eventual finite completion or timeout, displacement/yaw/gravity budget exits, no-progress/deadline continue effective, no new lowering exemption. Explicitly mark generated image cases synthetic CPU tests not original-env positive evidence. Keep detector mock compatible with new kwargs. Do not write results or run commands.

Return concise changed files and implementation caveats. Do not redesign the approach, tune standoff, change actuator limits/gains, add dependencies or propose a second planning cycle. Implement now.
