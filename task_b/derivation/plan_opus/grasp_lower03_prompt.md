Implement this small plan approved by GPT-6-Astra ultra. WRITE ONLY /home/lybm/ATEC_Robotics_Projects_20260910/task_b/grasp_probe.py. No other writes; no tests, GPU, Git, docs, memory or agents. Root owns evaluator and an independent agent owns tests. Finish promptly after minimal edits.

Current module is the audited .025 preload / .20 paired lift candidate, with bounded q2 feedback and all prior guards. It requires exactly lowering_m=.02. Enable one additional candidate .03 through the ALREADY SUPPORTED frozen FirstReach constructor. Do not edit FirstReach or arm_kinematics or add a new lowering state.

Exact changes:
1. Default remains .02. Rename misleading REQUIRED_LOWERING_M to DEFAULT_LOWERING_M=.02 and add ALLOWED_LOWERING_M=(.02,.03), or equivalent clear constants. Accept finite scalar lowering_m matching .02 OR .03 to1e-9; reject all other values including .025. Pass the chosen value unchanged to FirstReachPolicy. Do not silently clip or round it.
2. Preserve the existing wrapper prefix delegation exactly: given the SAME lowering_m, it must match frozen FirstReach tick for tick until normal reach_lowering_hold_complete. Every failure propagates, no score-based trigger.
3. Preserve every post-prefix behavior: existing child's achieved lowering alpha/reference is held; same wheel anchor; preload.025, delta.20, all actuator feedback/rate/tether/position/FK thresholds, finger slew, close target commanded gate, pause, base budget, phase deadlines and total budget stay unchanged. No new approach or lower state.
4. Update constructor errors, docstrings, describe/debug metadata so there are no claims of requiring exactly .02. Static metadata should record default.02 and allowed[.02,.03]. Per-instance describe/debug should record the actual child.lowering_m. Do not leave a static required_first_reach lowering_m .02 field falsely describing a .03 instance. Keep the accurate distinction: candidate selected through offline analysis, runtime uses public observations only.
5. No original environment, reward, asset, joint limit, physics or target-source changes. No simulator/object state/seed42/object IDs as policy inputs.

Reason for this candidate: .025/.20 still physically slid; official same-object mesh bottom rose<.5mm despite gripper37.5mm. Static original mesh/actual pose shows 10mm extra body lowering can place open fingers lower on shoulder without intersecting bottle hull;20-30mm would intersect. Frozen FirstReach already allows0..03 and scales its finite lowering reference accordingly, with original3s descent+2s hold and guards. Thus only accepting .03 in this wrapper is required, not rewriting the lowering logic. This is still an experiment, not a proven grasp.

Independent tests will verify default .02 original53checks, exactly same-parameter .03 FirstReach prefix and failure propagation, .03 metadata, permitted/rejected values, unchanged physical finger/lift commands and bounds. Do not write tests yourself.
