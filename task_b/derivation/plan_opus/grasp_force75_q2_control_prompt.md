Implement this bounded physical grasp candidate in `task_b/grasp_probe.py` ONLY. No shared helper edits, simulator/physics/reward edits, memory, tests, Git or GPU. Preserve the existing public evaluator interface and the entire parameter-matched FirstReach prefix.

1. Keep configured lowering=.03 available, paired lift delta=.20 rad and the original public-measured fixed lift goal. Default lowering compatibility must remain as currently implemented.
2. Set fixed finger preload=.075 m. Construct the finite physical targets [-.075,+.075] locally in this probe, without calling the shared helper whose .025 limit is a project-local design bound. Keep original physical joint stops, actuator configuration and action mapping untouched. Explicitly identify these as finite position references beyond the closed stop, not changed physics or measured force.
3. Finger slew remains .05 m/s. CLOSE minimum active=2.4 s, maximum=4 s; retain target-commanded and actual 10-sample stable nonempty-width requirements. Fully open .035 to -.075 nominally requires2.2 s; the extra .2 s permits a fresh width window.
4. Only q2 position feedback changes: gain3, raw correction cap +/-.12 rad, exact first-order correction filter tau=.10 s (weight=1-exp(-dt/tau)). Initialize filtered correction to zero once on entering LIFT; freeze history on pause. Other five axes retain their original goal command. LIFT's fixed measured goal is never moved to match a biased actuator command.
5. Only q2 measured-command tether becomes +/-.18 rad; other axes retain +/-.10. Command slew remains .10 rad/s. Intersect previous-command slew, each axis's measured-q tether and original hard limits; if intersection is empty, stop and hold the previous command. Do not silently relax any bound.
6. LIFT-only arrival uses measured goal error<.04, public pinch rise>=.015 m, original body/width conditions, an instantaneous arm qdot hard cap<=.12 rad/s, AND a full .5 s measured six-axis q window with total range<=.002 rad. Use ceil(.5/dt)+1 consecutive samples (26 atdt=.02) to cover both endpoints. Clear this history on phase change, pause or any failed prerequisite. Keep the original .3 s arrival duration after the window has become ready. Do not modify FirstReach's original qdot gate. Retain every original hard instability, empty-width, no-progress and deadline check.
7. LIFT maximum5 s, OBSERVE2 s; total probe maximum12 s. Original pause limits remain. OBSERVE continues the existing `_track_lift_goal` closed loop against the SAME fixed measured goal with q2 feedback/filter/slew/tether/hard limits. LIFT-to-OBSERVE does not reset filter history; only pause freezes history and commands. Clear the LIFT admission position window at the phase transition, without changing the original2 s observation semantics. Preserve closed finger targets, lower reference and same wheel anchor. No reward, object/root pose, contact force or privileged state may enter transitions.
8. Update every description/debug/constant and stale numeric message. Report q2 raw/filtered correction, selected tether, actual fixed-goal error, position-window duration/range, hard qdot threshold and clear/reset semantics. Do not claim physical grasp from normal policy completion, pinch FK rise, nonempty jaws or official proximity score.

Planning evidence (not policy inputs): original Task B mass override is .5 kg, attached arm K80/D4/effort limit100. Last physical q2 reference was continuously .10 rad below measured q, so raising P without changing that selected tether cannot reach the requested reference. Official .5 kg payload and complete USD arm mass/COM analysis bounds the closure pose gravity load near10.163 Nm (equivalent reference offset .12704 rad). Gain3 gives a conservative equilibrium goal error about .03176 rad. A12.4 Nm high-arm bound gives .03875 rad error and .11625 rad feedback, within the unchanged .12 correction cap. The .18 rad reference separation gives a nominal14.4 Nm spring term. Neither is a dynamic guarantee. Original bottle shoulder first-contact normals estimated from actual pose and static convex hull have22.3–25.5 degree upward slope; .075 preload gives a larger nominal lift margin than.025 while per-finger spring terms remain around8.5–8.7 N. Actual force/friction distribution remains unmeasured.

Independent checks (another agent owns them): unchanged parameter-matched FirstReach prefix; exact finger target mapping and slew; selected-axis feedback/tether isolation; filter initialization and pause freeze; infeasible-intersection safe stop; fixed goal unaffected by command compensation; position drift/oscillation rejects, emergency rate rejects, genuine micro-motion window admits; no stale-window admission; existing failure propagation. Real acceptance remains independent actual-object-quaternion/mesh bottom clearance and sustained follow, not policy flags.
