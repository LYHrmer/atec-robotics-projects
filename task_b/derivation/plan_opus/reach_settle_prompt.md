Implement this small, already-decided Astra ultra contract. Only edit /home/lybm/ATEC_Robotics_Projects_20260910/task_b/first_reach.py and task_b/audit_first_reach.py. Do NOT write or update any memory file, even your automatic project memory. No docs, other files, commands, simulations, Git or new design. Read these 2 files once, make targeted edits, return brief. Root runs tests. Avoid duplicate checks or large rewrites.

Actual latest run plan_p1_locked_seed42_01: arm DID reach <=.04rad error (last .01780), gripper object distance .213643m, base tangent speed max .0048m/s, no illegal contacts. Single instantaneous public speed/angular threshold triggered before a steady window, so 0 score. Last stopping public84 was missing from evaluator record; root has independently added that recording. No evidence justifies a threshold increase. Need bounded pause/recovery with same thresholds. This run still lowering=0; no P2 implementation.

1. In _reach change instantaneous stationarity violation vnorm>=.06 OR angular norm>=.12 into finite settle, only for real zero-lowering P1. State must remain REACH (wheel hold depends on it); debug reach_subphase=SETTLE_AFTER_MOTION. Freeze existing arm_command and lowering_alpha during settle, not snap to actual or goal. Track entry step, cumulative paused seconds, consecutive quiet duration. After BOTH original norms below threshold continuously .2s, resume original goal. Single settle episode >=2s OR accumulated settle >=3s finishes reach_settle_timeout / reach_settle_budget_exceeded. Counters cannot reset on each violating call, total never resets during same reach. Include bad and recovery-quiet ticks in duration.
2. Existing .03m tangent displacement/.05yaw/.05gravity budgets and original total reach deadline MUST execute on every call including settle. Existing act posture hard exits unchanged. Freeze progress-monitor anchor to current error while settling so a 2s settle doesn't itself count as a 3s commanded-motion stall. On leaving settle re-anchor progress. Preserve original measured q tether, .30 slew and all detector behavior. Record actual public linear vector and norm, angular vector and norm, tangent speed and settle cause/durations/quiet seconds in debug.
3. Final zero-lowering observation completion: q maxerror<.04 PLUS arm qdot max<.05 PLUS tangent speed<.01 PLUS original v/angular norms under thresholds must continuously hold 2s before reach_observation_complete. Reset final goal clock on any failure or settle. Don't finish solely on transient actual q hitting hard limit. Lowering>0 keeps its existing strict path unchanged this task.
4. CPU modify old immediate-base-motion expectation only for P1; lowering>0 original exit preserved. Add a few precise tests for a one-tick violation freezes then .2s quiet resumes without losing fixedgoal; prolonged violation exits in finite2s; repeated episodes exhaust3s; drift/deadline still execute during settle; transient q-at-goal with arm qdot above.05 cannot complete; settled q+quiet for2s completes. Keep original 50 checks except directly superseded P1 immediate-stop checks. Synthetic reach probe remains unaffected, and no false score claims. Fixtures should reset feed per scenario and simulate real arm qdot=0 for final settled window.

Do not touch the distant-component lowering branch in this task: single-factor P1 settle only. Return when implemented; do not run another planning or memory pass.
