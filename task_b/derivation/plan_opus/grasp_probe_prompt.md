Implement a single bounded close-and-small-lift probe, not delivery/navigation.

WRITE ONLY /home/lybm/ATEC_Robotics_Projects_20260910/task_b/grasp_probe.py. All other files, tests, memory, docs, configuration, official environment, Git and GPU are forbidden writes/actions. Read existing code as needed. Root owns evaluate.py and an independent agent owns the audit. Do not invoke agents. End with a short report and no simulation-success claim.

Interface: GraspProbePolicy(schema, observation_joint_names, defaults, dt=.02, *, the same keyword arguments as FirstReachPolicy). Expose act(proprio84, images), state, done_reason, debug, alpha, pause_for_stance, describe(), and boolean wheel_hold_requested. Reuse frozen FirstReachPolicy by composition or a small private subclass; do not edit it or duplicate its whole controller. This probe requires reach_only=False and lowering_m=.02. No reward, object/world state or simulator input.

Until FirstReachPolicy normally returns reach_lowering_hold_complete, actions and all failure semantics are identical to it. Any other done_reason propagates unchanged. Only that NORMAL completion enters the probe. Never trigger on score, reset a failure, relocate an object or re-solve a GT target. Root runs score_hold_steps=0. Keep the existing lowered leg reference alpha=1 and the same wheel anchor continuously through CLOSE, LIFT and OBSERVE. Do not raise the base or release/re-anchor wheels. No extra locomotion.

Fixed constants for this first candidate:
- finger close target = task_b.arm_kinematics.gripper_targets(0, preload_m=.015), physical q7=-.015m and q8=+.015m. Use arm_targets_to_action with the original defaults/scale/order; do not modify physical joint limits/actuators. As existing Task E does, finite target preload is beyond the closed stop, with real motion constrained by original physics.
- close target slew <=.05m/s per finger, other six arm commands held. Use the previous actual command as command start; no abrupt close.
- lift goal is latched from PUBLIC MEASURED arm q after closure: q2 -= .10rad, q3 += .10rad, all other arm angles unchanged. Retain closed finger target. Check static limits before moving, never silently clip an unreachable goal.
- lift command per-axis rate <=.10rad/s, measured-state tether q+/- .10rad. No Cartesian IK or grasp-point shift in this probe.
- quiet/public motion thresholds are unchanged: norm(v)<.06m/s, norm(omega)<.12rad/s; hard tilt .25, reach instability tilt>.12 or roll/pitch rate norm>.45 stops. From probe entry retain tangent displacement budget .03m, yaw .05rad and gravity-direction change .05rad, computed only from public twist/gravity. Bounds continue during pauses. The existing achieved lowered reference remains fixed.

States:
1) PROBE_CLOSE, maximum 3s. Keep the six arm joints at their previous achieved hold command; gradually close fingers. Record measured jaw width q7-q8. After at least 1.4s of active closing/holding, require ten consecutive 50Hz measurements with width range<.002m and median width in [.006,.065]m. A stable nonempty width only permits the bounded lift; it is NOT a claim of contact/grasp. If width<.006m for .2s after the close target has been commanded, stop grasp_probe_empty_close. If the response never settles before 3s, stop grasp_probe_close_not_confirmed. Do not repeatedly squeeze/retry. Do not use score or privileged contact sensors for this decision.
2) PROBE_LIFT, maximum 5s. Latch measured q once, make the fixed paired joint goal above, and move smoothly from current commands under speed/tether bounds. Track actual q error, qdot and public body-frame FK. Require actual max goal error<.04rad, arm max qdot<.05rad/s and measured pinch displacement along public up>=.015m continuously .3s before OBSERVE. The expected paired-joint lift is approximately 28mm for the recorded pose, but do not hardcode or claim that as actual motion. If a full 2s active window has <.01rad improvement while max error>.04, stop no_joint_lift_progress; if no arrival by 5s, stop grasp_probe_lift_not_reached. A jaw width collapse<.006m for .2s after closure stops grasp_probe_empty_during_lift.
3) PROBE_OBSERVE, 2s. Hold the same measured-lift goal and closed target, with the same wheel/leg reference and all motion bounds. Empty-width collapse still stops. End normally as grasp_probe_observation_complete. Never call it grasp_success; image/GT/object motion are not inspected by this probe to certify success. This experiment ends after the observation, without transport, opening/regrasp or additional lowering.

Finite pauses: if public norm(v)>=.06 or norm(omega)>=.12, freeze finger/arm commands, keep the lowered reference and wheel anchor. Resume after .2s continuously quiet. At most 2s per episode and 2s cumulative across the entire probe; then stop with an explicit reason. Reset stable-width/arrival/observation quiet windows after a pause, but never reset a phase deadline or the common body-motion budget. The total probe budget is 10s from normal first-reach completion, including pauses. Invalid public84 or non-finite state stops immediately. Arm/finger observations must use resolved observation joint names, not articulation order.

Diagnostics: probe phase, entry global call, phase elapsed/remaining, pause counters, original held arm command, measured close q7/q8/width window, physical finger targets, latched lift q, paired delta, actual max q error/qdot, public FK pinch displacement, wheel_hold_requested, and body budgets. Preserve child debug/describe and clearly identify fixed candidate constants. FirstReach prefix actions and failures must stay identical; no step gets two calls to the inner policy.

Independent CPU checks (do not write them): unchanged first-reach prefix/failure propagation; no reward/GT access; preload affine mapping and bounded finger slew; real jaw-width range gating/repeated values; measured q latch and paired lift with limits/rate/tether; invalid-input/body/pause/deadline handling; continuous wheel hold and unchanged lowering; normal reason says observation only.

Context only, never controller input: Offline winning actual B-mount FK + nominal bottle orientation and static original convex hulls indicate an open 70mm jaw can surround the upper bottle, with bilateral hull contact possible around 50mm opening; actual bottle quaternion was not saved for that old run, and static overlap is not a friction/grasp guarantee. Root is adding future object/gripper quaternion diagnostics outside policy. The reference Task E close API is allowed as static code reference, but its table coordinates and score-based lift verification are NOT applicable to Task B.

Real validation is external: fresh original-environment run, same first-score parameters, mode grasp_probe, score_hold_steps0, new output directory. Audit the SAME object's true pose and mesh lower surface using recorded orientation: after the small lift its root should rise>=.015m, its lowest surface should clear its pre-close support level by>=.01m, and it should follow the gripper for at least 1s with nonempty jaws and without falling back. Compare before/after and relative pose, not just score. Even a positive grasp-probe result does not prove transport, delivery or Task B completion. GPU remains root-only and sequential with multi_reach.
