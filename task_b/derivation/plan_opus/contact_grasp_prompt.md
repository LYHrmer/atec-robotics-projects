Implement ONE small, bounded experimental module: `task_b/contact_grasp.py` ONLY. Do not modify any other file, including tests, memory, CLAUDE.md, existing policies, physics, rewards or evaluation. Do not launch Isaac/GPU, Git or network. Read existing files as needed. Prefer ~300 lines; no copied 1000-line GraspProbe framework. The independent auditor and evaluator are other agents' ownership.

Class/API: `ContactGraspPolicy(GraspProbePolicy)`, same constructor kwargs/public interface as current `GraspProbePolicy`, but default and ONLY accepted `lowering_m=.02` (finite scalar). Call its constructor unchanged with .02. Keep `act(proprio, images)`, `state`, `done_reason`, `debug`, `describe()`, `alpha`, `pause_for_stance`, `wheel_hold_requested`. FirstReach and GraspProbe are frozen read-only. A subclass can intercept `_prefix`, dispatch `_probe`, extend `_action`, `_record`, `describe` while reusing inherited act/call count, safety helpers and the entire original CLOSE/LIFT/OBSERVE. No monkeypatch or global constant mutation.

Reason: two actual fingers contact the sloping mustard shoulder; force75 raises the gripper but the object remains grounded and slips out. Actual shapes have mu=1, mass=.5kg; these are offline evidence, NOT policy inputs. New candidate is frozen FirstReach(.02), fully OPEN, q6 relative +30 degrees, then an additional .02 m nominal leg reference, then the existing force75 close/lift. Total nominal lowering=.04, but no actual-body-height claim. Original FirstReach constructor still only accepts <=.03 and is NOT changed. Actual runtime uses only public proprio/RGBD inherited by FirstReach; no score/reward/contact/net_force/object pose/GT/map/seed inputs.

1. Exact prefix and handoff.
Call super `_prefix` once; return its exact action unchanged on EVERY prefix call, including normal completion. Every non-normal stop propagates unchanged. Only normal `reach_lowering_hold_complete` changes the newly entered inherited PROBE_CLOSE into `CONTACT_OPEN` before the next action. Record a new contact-entry call and PUBLIC gravity anchor. Keep inherited original wheel anchor continuously requested through all added phases and the inherited probe; never release/re-engage it. Do not alter any frozen child fields. Keep the inherited post-prefix arm command. Set parent `entry_call=None` while preparing so its 12 s probe timer has not begun. At later CLOSE entry set `entry_call=phase_start=calls`; that starts only the existing probe clock, not body/pause/global budgets. All q6/lift references are measured public q; future inherited `_enter_lift` latches the NEW, rotated/deeper actual q normally.

2. CONTACT_OPEN, maximum 1.0 s.
Slew physical finger commands toward q7=+.035/q8=-.035 m with original .05 m/s limit, from previous command, while retaining all six arm and leg commands. Require actual jaw width >=.066 m for a complete .2 s consecutive interval and finger q7>=.032, q8<=-.032. Never claim this proves collision clearance. Deadline failure `contact_open_not_reached`. This candidate's fingers are already open in the original prefix; the phase verifies current observations. Only on success latch fixed wrist goal = PUBLIC actual q6 + pi/6 and reject if outside original q6 hard limits. Store other five held arm commands unchanged.

3. CONTACT_ROTATE, maximum 7.0 s including pauses.
Only q6 changes; fixed goal never chases actual q. Command slew <=.10 rad/s and |command-actual q6|<=.10 rad, intersect both with original hard limits. Empty intersection stops with previous valid command (`contact_wrist_command_bounds_infeasible`), never sequential clipping/jumping. Keep all other arm commands, open fingers and achieved child leg reference. Fresh arrival evidence: |actual q6-goal|<.04 and max abs actual arm qdot <=.12, actual held-arm errors of other axes <.04, body tangent speed<.01, raw linear norm<.06, angular norm<.12, tilt<=.10; a FULL .5 s actual six-arm-position window (ceil(.5/dt)+1 samples, 26 at .02) must have each span<=.002 rad. The corresponding leg window must have max per-axis span<=.02 rad. Any ineligible sample clears the window. Clear it on phase change or pause. Require open width>=.064 throughout; if smaller for .2 s stop `contact_open_width_lost` (do not close/lift). q6 no-progress: after 2 active seconds, if fixed-goal error remains>.04 and improves <.02 rad, stop. Deadline `contact_wrist_not_reached`.

4. CONTACT_LOWER, maximum 5.0 s including fresh admission, motion, settling and pauses.
Keep wrist at the valid last rotated command, other five held commands unchanged, fingers open. Independently reacquire a fresh .5 s arm/leg/body quiet window with the same conditions as step 3 before authorizing descent; never reuse the frozen FirstReach gate or prior phase's window. At authorization latch current PUBLIC leg q in action leg-name order once. Additional fixed physical delta is an unchanged copy of child.lower_delta: because the child lowering parameter=.02 this is precisely another nominal .02 reference. It is NOT a new world-height measurement. Validate public start leg q and start+extra_delta inside original leg hard limits (hip[-.87,.87], thigh[-.94,4.69], calf[-2.82,-.43]). Keep a separate `contact_extra_alpha` initially0, advance at most dt/3 toward1 on active body-quiet ticks while arm actual errors<.04 and width>=.064; freezes during pauses or invalid arm readiness. In overridden `_action`, call parent and ADD `extra_alpha * extra_delta / leg.scale` on the leg slice. Parent already supplies its completed .02 delta; do not replace it or count it twice. Prefix action bypasses this addition unchanged. Additional alpha/reference must persist through CLOSE/LIFT/OBSERVE and all stops.
Track actual increment error=maxabs((current_leg_q-latched_leg_q)-alpha*extra_delta). Error>.06 rad continuously .2 s -> stop `contact_lower_increment_error`. After alpha>=.5 for1 s, require projected joint progress beta=dot(q-q0,extra_delta)/dot(extra_delta,extra_delta)>=.15 or stop `contact_lower_no_joint_progress`. These measure public joint response, not actual chassis height. At alpha1 require another fresh FULL .5 s current arm/leg/body quiet window before CLOSE; do not include moving descent samples. Max actual leg-position span<=.02. Deadline `contact_lower_not_reached`. Width-loss watchdog remains active.

5. Inherited probe, unchanged.
Only after all prep completes set phase/state PROBE_CLOSE, held_arm_command=current six-arm command; reset ONLY phase-specific CLOSE width/counter/empty evidence (already unused) and probe entry/phase clocks. DO NOT reset base displacement/yaw/gravity anchor, cumulative pause counts, contact entry or extra alpha. Current original force75 semantics remain exactly: finger target[-.075,+.075], close2.4/4s, paired measured lift q2-.20/q3+.20, q2P3/cap.12/tau.1/tether.18, other tethers.10, arm rate.10, fixed actual goal .04, FK15mm, original .5s arrival gate and2s observe; inherit rather than reimplement. LIFT reference/R0 derives NEW actual q after rotated/deeper closure. The parent action extension keeps extra legs at1 throughout.

6. Safety/timing, once per post-prefix tick.
Reuse original public finite84 validation, gravity validity, tilt>.25 hard stop, tilt>.12 or roll/pitch rate>.45 instability stop, raw vnorm .06 / omega .12 bounded pause and .2s recovery. Preserve original episode2s/cumulative2s pause across prep+probe; freeze commands/alpha and clear fresh windows during pause. `_budgets` must run once each post-prefix tick across ALL phases (including pauses), with the ORIGINAL displacement .03 / yaw .05 / gravity-angle .05 bounds and same anchor from contact entry. Inherited `_probe` already does this for CLOSE/LIFT/OBSERVE, so don't double-integrate it there. New contact path calls it exactly once. One global `contact_total_s=25` deadline from first normal prefix completion through final inherited observe, including all pauses. Individual phase deadlines remain; no fallback retry, re-anchoring, restarting or phase-time extension. On all failures hold last valid action and stop. No user approval loop or further plan needed.

7. Diagnostics and truthful description.
Expose debug/describe: contact_phase (OPEN/ROTATE/LOWER then inherited phase or equivalent full names), contact_entry_call, contact_elapsed_s/remaining, first_reach_lowering_m=.02, extra_lowering_m=.02, total_reference_lowering_m=.04, contact_extra_alpha, wrist actual start/goal/command/error, quiet samples/span/required samples, lower authorization call/start public q/increment error/beta, phase deadlines, global25s, preserved body/pause budgets, wheel hold. Fix inherited text that otherwise says no extra lowering or no wrist movement. Do not falsify child frozen metadata: inherited child .02 alpha1 stays true, additional reference is separate. Before CLOSE probe clock must truthfully show not started. After CLOSE keep parent probe/debug fields for existing auditors. The module remains an experimental contact candidate, not a grasp/delivery proof.

8. Meaningful local checks only.
No shell tools are enabled; root will run py_compile for the new file. Independent agent will black-box test exact FirstReach(.02) prefix, failure propagation, fully open admission, measured relative q6 goal, only q6 motion, rate/tether/hard intersection and empty stop, fresh full .5s windows, pause freeze/unchanged deadlines and body anchors, extra delta unit/mapping plus persistence, no closed commands before completion, new actual q lift latch, total25s and original probe semantics. Do not write tests or other files yourself. Summarize actual code changes and validation; do not claim GPU outcome.

Implementation detail: initialize subclass fields safely before any super-constructor dynamic dispatch into overridden _record/_action, or guard uninitialized fields. Preserve original GraspProbe/FirstReach files byte-for-byte.
