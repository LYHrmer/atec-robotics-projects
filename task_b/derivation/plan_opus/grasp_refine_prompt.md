Execute this narrow plan from GPT-6-Astra ultra. WRITE ONLY /home/lybm/ATEC_Robotics_Projects_20260910/task_b/grasp_probe.py. Do not write any other module, tests, docs, memory, original environment, configuration or Git. No GPU or agents. Root integrates, independent agent tests. Read only the needed code and finish promptly with a short report.

Current module is the existing working GraspProbePolicy with bounded q2 feedback. Make these precise changes:
1. CLOSE_PRELOAD_M = .025. Keep gripper_targets(0., preload_m=CLOSE_PRELOAD_M), targets q7=-.025/q8=+.025 metres, original helper allowed range unchanged.
2. PAIRED_DELTA_RAD = .20. Latch public measured q after stable closure exactly once; q2 -= .20/q3 += .20; other four arm angles unchanged, static hard-limit goal check remains.
3. Keep finger slew .05m/s, close minimum active1.4s, maximum3s, 10-consecutive-sample stable nonempty width window. Require closed target has actually been commanded before allowing lift (if existing logic already requires it, preserve it). Nominal .035 to -.025 needs1.2s, plus10sample .2s. Never weaken the measured width gate.
4. Preserve q2 feedback gain1/cap.08, arm rate.10rad/s, measured tether +/-.10rad and original hard-limit intersection. Preserve fixed actual-goal error<.04, actual qdot<.05 and pinch rise>=.015m sustained.3s. LIFT5s, OBSERVE2s and total10s are unchanged, as are every pause/empty/base-motion guard.
5. Frozen FirstReach prefix, its failure propagation, lowered leg alpha, continuous wheel anchor, and public-only inputs remain unchanged. No reward, score, object poses, IDs, contact flags or simulator state inputs.
6. Make all comments, describe/debug/metadata strings consistent with constants. Remove stale claims of .015 preload or .10 paired delta; use formatted constants for these descriptions to avoid future drift. Candidate name should say bounded clamp/lift, never grasp success. Do not change the separate .10 rate or .10 tether values. Remove description saying this is exactly the TaskE close, since preload differs. Explain finite position-target preload requests holding force while original physics/stops stay unchanged.

Evidence for context only: original TaskB Mustard_cfg mass=.5kg, attached Piper actuator stiffness80/damping4. TaskB disables base physics_material startup event; default material static/dynamic1, multiply. Do not claim actual contact force from spring estimates. Current .015 preload/.10lift real run raises gripper17mm but bottle mesh bottom under.5mm, hence no verified lift. Static original FK with actual start q permits paired .20 within hard limits, ideal gripper rise56.6mm upward. This new candidate is an experiment, not proven grasp. Never modify environment or helper range.

Final real verification remains external same-object actual quaternion/mesh bottom clearance and follow for1s. End after the code edits with a concise report; no independent execution tools are enabled.
