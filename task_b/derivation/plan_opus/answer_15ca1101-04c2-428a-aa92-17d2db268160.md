Done — only `task_b/grasp_probe.py` was written.

## Changes

**Constants**
- `CLOSE_PRELOAD_M .015 → .025` (comment now just states q7=-.025 m / q8=+.025 m). `gripper_targets(0., preload_m=CLOSE_PRELOAD_M)` call and its `[-.025, +.025]` assertion are unchanged in form; .025 is the top of the helper's existing allowed range, so `arm_kinematics.py` was not touched.
- `PAIRED_DELTA_RAD .10 → .20`. Latching (`_enter_lift`: once, from public measured `q[:6]`, `q2 -= δ`, `q3 += δ`, other four unchanged) and the static hard-limit check with `grasp_probe_lift_goal_outside_joint_limits` (never silently clipped) are untouched.

**Lift gate (item 3)**
- `_close` now requires `self.close_target_commanded` in addition to `CLOSE_MIN_ACTIVE_S` and the stable window: `if close_active_s >= 1.4 and close_target_commanded and stable`. Previously the 10-sample window plus 1.4 s could in principle admit the lift without the closed target having been commanded; the width gate itself is unchanged (10 consecutive samples, range < .002 m, median in [.006, .065] m), slew .05 m/s, max 3 s.

**Untouched, verified by inspection:** q2 feedback gain 1.0 / cap .08, `LIFT_RATE_RAD_S = .10`, `LIFT_TETHER_RAD = .10` and the rate∧tether∧static-limit simultaneous intersection, arrival `error < .04 ∧ qdot < .05 ∧ pinch_rise >= .015` held .3 s, LIFT 5 s / OBSERVE 2 s / TOTAL 10 s, all pause / empty-width / base-displacement / yaw / gravity guards, frozen FirstReach prefix and its failure propagation, `lowering_alpha` leg reference, continuous wheel anchor, public-proprio-only inputs.

**Strings/comments (item 6)**
- Preload and paired-delta descriptions now interpolate `CLOSE_PRELOAD_M`, `PAIRED_DELTA_RAD` (and the close/lift/pause/budget thresholds in `STOP_REASONS` and `describe()['phases']`) instead of hardcoded literals, so they can't drift. The `.10` rate and `.10` tether values are unchanged.
- Removed "as Task E does" / "exactly as the existing Task E close does". The preload note now says: a finite position target .025 m beyond the closed stop requests holding force from the *unchanged* position actuators, with original joint limits, stops, actuator stiffness/damping and the helper's allowed preload range untouched, so actual motion and any actual force stay whatever the original physics gives. No spring-estimate force claim anywhere.
- Candidate name is now "bounded clamp-and-small-lift candidate; an experiment, not a grasp success…"; the module docstring adds "This candidate is an experiment, not a proven grasp" and keeps the external-audit-only verification claim. The stale "FIRST candidate" header comment was retitled.
- `grasp_probe_close_not_confirmed` now reads "the closed target was not commanded, or no stable non-empty width window formed, inside 3 s", matching the new gate; a new metadata flag `close_target_commanded_before_lift: True` records it.

## Notes for the integrator
- No execution tools were enabled here, so nothing was run — not even a syntax/import check. Edits were made against the read source only.
- `task_b/audit_grasp_probe.py` (not mine to modify) hardcodes the old values at lines 221 (`[-.015, .015]` finger target), 280/284/309 (`±.10` paired delta) and 313; those expectations will now fail and need the independent agent to update them to .025 / .20. The `.10*DT` rate and `.10` tether assertions at 225/342/405 and the `>= .015` rise at 313/414 remain correct.
- Timing is exactly critical: nominal slew 1.2 s + .2 s window = 1.4 s = `CLOSE_MIN_ACTIVE_S`, so if the first reach leaves the fingers wider than .035 m the closed target is commanded later than 1.2 s and the added gate delays lift entry accordingly (still bounded by the 3 s close deadline).
- Real verification is still external: a fresh original-environment run plus an independent audit of the same object's recorded quaternion/mesh-bottom clearance, followed for 1 s.